[project]
name = "infralens"
version = "0.1.0"
description = "AI-powered Infrastructure Analysis Platform — Phase 1: repository scanning backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9.0",
]

[tool.uv]
package = false